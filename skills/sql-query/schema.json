{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "sql-query input",
  "description": "Input schema for the sql-query skill.",
  "type": "object",
  "properties": {
    "operation": {
      "type": "string",
      "enum": ["query", "tables", "describe"],
      "description": "The operation to perform."
    },
    "args": {
      "type": "object",
      "description": "Arguments for the chosen operation.",
      "properties": {
        "dsn": {
          "type": "string",
          "description": "Data source name. Format: sqlite:///path/to/db.sqlite or postgres://user:pass@host:port/dbname"
        },
        "sql": {
          "type": "string",
          "description": "The SQL query to execute (query operation only)."
        },
        "params": {
          "type": "array",
          "items": {},
          "description": "Optional positional parameters for the SQL query."
        },
        "table": {
          "type": "string",
          "description": "Table name to describe (describe operation only)."
        }
      },
      "required": ["dsn"]
    }
  },
  "required": ["operation", "args"],
  "additionalProperties": false
}
