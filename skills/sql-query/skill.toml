[skill]
name = "sql-query"
version = "0.3.0"
description = "Execute SQL statements against SQLite or PostgreSQL and return structured results."
license = "Apache-2.0"

[runtime]
kind = "rust"
entrypoint = "src/main.rs"
determinism = "io-bounded"

[interfaces]
mcp  = true
cli  = true
http = true
lib  = true

[publish]
registry = "ghcr.io/zenmicro-tech/skillforge/skills"
