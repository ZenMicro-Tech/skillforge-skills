[skill]
name = "git"
version = "0.1.0"
description = "Git operations: status, diff, log, commit, branch, checkout, and more."
license = "Apache-2.0"

[runtime]
kind = "rust"
entrypoint = "src/main.rs"
determinism = "io-bounded"

[interfaces]
mcp  = true
cli  = true
http = true
lib  = true

[publish]
registry = "ghcr.io/zenmicro-tech/skillforge/skills"
